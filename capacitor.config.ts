import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ramin.starter",
  appName: "Ramin Starter",
  webDir: "build/client",
};

export default config;
